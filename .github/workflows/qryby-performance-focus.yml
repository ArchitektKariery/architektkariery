name: QRyby performance focus
on:
  push:
    branches: [main]
    paths: ['tools/qryby_performance_focus.py']
permissions:
  contents: write
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 tools/qryby_performance_focus.py
      - run: |
          git config user.name "qryby-perf-audit"
          git config user.email "actions@users.noreply.github.com"
          git add docs/qryby-performance-focus.txt
          git commit -m "Focus QRyby performance audit [skip ci]"
          git push origin HEAD:main
