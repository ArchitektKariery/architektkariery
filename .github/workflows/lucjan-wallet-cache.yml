name: Lucjan wallet sync cache
on:
  push:
    branches: [main]
    paths: ['tools/lucjan_wallet_cache.py']
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: python3 tools/lucjan_wallet_cache.py
      - run: |
          git diff --check
          grep -F 'community-restoration-live.js?v=20260926-live3' qryby.html
          node --check src/lucjanek/community-restoration-live.js
      - run: |
          if git diff --quiet -- qryby.html; then exit 0; fi
          git config user.name "qryby-wallet-fix"
          git config user.email "actions@users.noreply.github.com"
          git add qryby.html
          git commit -m "QRyby: load Lucjan wallet sync fix [skip ci]"
          git push origin HEAD:main
